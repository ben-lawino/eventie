import 'package:flutter/material.dart';

class CompletedEventScreen extends StatelessWidget {
  const CompletedEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Completed Events')),
    );
  }
}
