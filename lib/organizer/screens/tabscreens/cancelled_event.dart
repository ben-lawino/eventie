import 'package:flutter/material.dart';

class CancelledEventScreen extends StatelessWidget {
  const CancelledEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Cancelled Events')),
    );
  }
}
